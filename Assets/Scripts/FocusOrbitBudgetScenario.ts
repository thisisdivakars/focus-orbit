import {Scenario} from 'Leaf.lspkg/Scenarios/scenario/Scenario';import {expect} from 'Leaf.lspkg/Utils/common/Expect';import {findSceneObjectByName,sleep} from 'Leaf.lspkg/Utils/common/Utils';import {tap} from './FocusOrbitLeafTestUtils'
@component export class FocusOrbitBudgetScenario extends Scenario{async run(){await sleep(1500);await tap('Task-description');await tap('Action-FOCUS');await tap('Task-assets');await tap('Action-FOCUS');expect(findSceneObjectByName('Task-assets')).not.toBeNull();expect(findSceneObjectByName('AttentionBudgetMessage')!.enabled).toBe(true)}}
